/**
 * @deprecated — Old PHP endpoint strings. Replaced by Next.js API routes.
 *
 * New route structure:
 *   /api/auth/*          — authentication
 *   /api/admin/*         — admin operations
 *   /api/products/*      — product catalog
 *   /api/inventory/*     — inventory & batches
 *   /api/orders/*        — orders
 *   /api/deliveries/*    — deliveries
 *   /api/customers/*     — customer management
 *   /api/staff/*         — staff management
 *   /api/drivers/*       — driver management
 *   /api/notifications/* — notifications
 *   /api/audit/*         — audit logs & login history
 *
 * This stub exists only to prevent import errors during migration.
 */

export {};
