/**
 * DEPRECATED — mock products removed.
 * Real product data comes from GET /products (public) and GET /admin/products (staff).
 * Safe to delete once all pages are wired to the real API.
 */
export {};
