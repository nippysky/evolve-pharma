/**
 * DEPRECATED — all mock data has been removed.
 * Real data comes from the API (auth/me, customer/*, staff/*, etc.).
 * File kept to avoid breaking any residual imports during migration.
 * Safe to delete once all pages are wired to real API endpoints.
 */
export {};
